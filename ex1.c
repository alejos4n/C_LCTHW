#include <stdio.h>
/*la extensión .h es por headers*/
/* esta es una forma de comentario */
int main(int argc, char*argv[])
{
	//otro comentario
	// inicializo la variable distance
	int distance = 100;
	printf("usted está %d km de distancia", distance);
	// con %d y ,distance; estoy indicando que en el %d irá el valor de la variable distance
	return 0;
}
